#set page(margin: 24mm, fill: white)
#set text(size: 11pt)

= Typst Preview Check

If you can read this text, the Typst preview is rendering correctly.

This is a minimal document intentionally kept simple.
